# Examples

The Angular applications under the repository-level `examples/` directory exercise basic discovery, Router, DI, Signal Forms and NgRx. `pixi-app` exercises official PixiJS DevTools registration, scene traversal, renderer discovery and managed textures. These are adapter fixtures, not assumptions of the generic ADP packages.
