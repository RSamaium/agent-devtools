# RFC-0006: MCP

**Status:** Accepted

The MCP server exposes generic `adp_*` tools for status, snapshots, queries, events, diff and explain. The reference adapter adds `angular_*` tools for its domains. Tool schemas validate pagination, budgets and references before calling the shared client.
