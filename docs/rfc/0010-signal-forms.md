# RFC-0010: Angular Signal Forms

**Status:** Accepted

Classic and Signal Forms share the `forms` domain while retaining distinct resources. Signal Form snapshots contain model, fields, validation state, errors and optional schema evidence. Runtime migration plans and assertion generation are Angular adapter assistance, not generic core behavior.
