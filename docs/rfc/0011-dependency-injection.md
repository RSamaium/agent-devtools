# RFC-0011: Angular dependency injection

**Status:** Accepted

Injectors and providers belong to `dependency-injection`. Capture must not instantiate providers. The read-only `resolve` domain command explains an observed path from a component or injector and returns confidence and an optional winner.
