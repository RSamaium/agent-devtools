# RFC-0005: CLI

**Status:** Accepted

`agent-devtools` provides generic session, status, snapshot, query, watch, diff and explain commands. The Angular composition contributes framework-oriented commands without changing ADP wire methods. JSON output is stable on stdout; operational errors use stderr and documented exit codes.
