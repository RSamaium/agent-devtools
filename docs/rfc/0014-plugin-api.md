# RFC-0014: Dynamic Plugin API

**Status:** Draft V2

V2 may standardize external package discovery, installation, permissions, command contribution manifests and lifecycle isolation. V1 exposes only the minimal runtime adapter contract required by official compositions and does not dynamically install third-party code.
