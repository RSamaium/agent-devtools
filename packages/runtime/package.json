{
  "name": "@ng-agent/runtime",
  "version": "0.1.0",
  "description": "Main-world Angular runtime bridge for ng-agent",
  "type": "module",
  "sideEffects": false,
  "exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" }, "./browser-script": "./dist/browser/browser-entry.global.js" },
  "files": ["dist"],
  "scripts": { "prepack": "pnpm build",
    "build": "tsup src/index.ts --format esm --dts --clean && tsup src/browser-entry.ts --format iife --global-name NgAgentRuntime --out-dir dist/browser --minify",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": { "@ng-agent/protocol": "workspace:^" },
  "devDependencies": { "tsup": "^8.5.0", "typescript": "catalog:", "vitest": "^3.2.4" },
  "publishConfig": { "access": "public" },
  "license": "MIT"
}
