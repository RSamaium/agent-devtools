# PixiJS example

PixiJS 8 fixture registered through the official `@pixi/devtools` API. It provides a named stage, nested scene nodes and a managed texture for browser smoke tests.

```bash
pnpm --filter pixi-app start
pnpm test:browsers:pixi
```
