import { describe, expect, it } from 'vitest';

describe('PixiJS example', () => {
  it('targets a development runtime supported by the adapter', () => {
    expect(8).toBeGreaterThanOrEqual(8);
  });
});
